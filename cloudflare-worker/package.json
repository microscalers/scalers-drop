{
  "name": "microscalers-cli-worker",
  "version": "1.2.0",
  "description": "Cloudflare Worker for Microscalers CLI API",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231025.0",
    "wrangler": "^3.0.0"
  }
}